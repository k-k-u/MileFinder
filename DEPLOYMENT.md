# 公開構成と管理PCの運用

MileFinderは、Sitesの画面・Cloudflare Worker・D1と、ANA会員画面へログインした管理PCを組み合わせます。スマートフォンや別のPCにANAの認証情報を移す必要はありません。

| 機能 | 実行場所 | 管理PCの起動 |
| --- | --- | --- |
| 路線候補・必要マイル計算 | 利用者のブラウザ | 不要 |
| ANA匿名チャットの空席照会 | 公開Worker | 不要 |
| 固定アカウントの会員照会 | 管理PC上のログイン済みANAタブ | 必要 |
| 会員照会要求・検証済み結果の中継 | WorkerとD1 | 処理には必要 |

匿名回答と会員回答は区別します。匿名チャットのマイルは公開チャートによる計算値です。会員回答のマイルと税金等は、公式が返した指定人数分の往復合計を使います。どちらも部分取得・発券未確認として扱い、取得失敗や未照会を空席なしへ変換しません。

## ANAアカウントの境界

管理者がANA公式へログインし、対応する国際往復・エコノミー・成人のフライト一覧を表示してから、ローカルのMileFinderへ接続します。公開画面から接続する路線は、管理PCが接続した路線に固定します。ANAの会員番号・パスワード・Cookie・フォーム状態・応答HTMLはANAタブ内で扱います。

ANAタブはlocalhostのMileFinderとのみ `postMessage` で接続します。公開サイトのURLをブックマークレットの送信先へ置き換えません。管理PCからWorkerへの通信には、ANAの認証情報とは別の管理用トークンを用い、検証済みの便・日付・席数・合計料金・確認時刻などだけを送ります。管理用トークンも公開JavaScript・Git・ログ・URLのクエリへ埋め込みません。

管理PCの停止・スリープ、タブの終了、ANAのログイン切れ、画像認証、接続期限により会員処理は停止します。公開サイトの継続稼働と、会員セッションの継続利用は別です。ANAの画面変更や連続照会の失敗は未解決の運用制約であり、クラウドへ中継しても安定取得を保証しません。

## Sitesの保存と配備

配備先は [MileFinder](https://milefinder-ana-awards.raisin7524.chatgpt.site) です。URLの払い出しだけでは配備成功を意味しません。初回公開時は以下のビルド・保存・配備と、公開先での確認まで行います。

既存の `.openai/hosting.json` にある `project_id` を再利用します。同じサイトを再作成しません。Sitesの管理設定には論理的なD1のbinding名を指定し、実際のデータベースはSitesが管理します。設定例は `{"project_id":"既存のID","d1":"DB","r2":null}` です。ここに認証情報を保存しません。[Sites公式説明](https://learn.chatgpt.com/docs/sites)

ビルド対象のソースをコミットし、Sitesに設定されたリモートブランチへpushしてからバージョンを保存します。`save_site_version` の `commit_sha` は、そのリモートのHEADとアーカイブの元ソースに一致させます。保存と本番配備は別の操作です。

```powershell
npm ci
npm run build:cloud
npm run preview:cloud
```

`build:cloud` は型チェックと `vite.cloud.config.ts` のビルドを実行します。Worker入口は `server/cloudWorker.ts`、静的ファイルは `ASSETS`、D1は `DB` のbindingを使います。`preview:cloud` は生成済みの `dist/server/wrangler.json` を使い、[http://127.0.0.1:8791/](http://127.0.0.1:8791/) で動かします。ソースからの開発には `npm run dev:cloud` を使います。

この構成のD1のダミーIDはローカル開発用です。Sitesの実DBの識別子やAPIトークンを `vite.cloud.config.ts` へ埋め込みません。`MEMBER_WORKER_TOKEN` はSitesの秘密環境変数として登録します。値を変更した場合は、対象の保存済みバージョンを再配備して反映します。ANAの会員認証とは別の値であり、公開利用者へ渡しません。

アーカイブにはソースツリー全体ではなく、`dist/server/index.js` とWorker設定、`dist/client/` の静的アセット、`dist/.openai/hosting.json` を含めます。公式Sites Vite pluginは設定ファイルを `dist/.openai/hosting.json` へ、任意のDrizzle migrationを `dist/.openai/drizzle/` へコピーします。Worker入口の選択はこのpluginの役割ではなく、Cloudflare側のビルド設定で行います。[公式plugin](https://github.com/openai/sites/blob/main/packages/sites-vite-plugin/README.md)・[公式template](https://github.com/openai/sites/blob/main/packages/create-sites/templates/vinext/vite.config.ts)

保存されたversion IDを指定して配備し、終端の成功状態と公開URLを確認します。API、データベース、匿名チャットの実通信、管理PCとの往復を別々に確認します。ローカルの単体テスト・模擬E2Eが成功しても、Sites上での実通信成功とは扱いません。

## 管理PCの接続

1. 管理PCで `npm run dev` を実行し、[管理用接続画面](http://127.0.0.1:5173/?member-worker=1) を開きます。「公開サイトの会員検索を接続」をブラウザのブックマークバーへ保存します。
2. ANA公式でログインし、対応する路線の往復・エコノミー・成人のフライト一覧を表示します。保存した「公開サイトの会員検索を接続」を実行すると、ANAタブと接続した新しい管理画面が開きます。
3. 管理画面は管理PCに暗号化保存した設定の読み込みを1回試し、取得できれば自動接続します。取得できない場合は「ワーカートークン」欄へ管理用トークンを入力し、「接続を開始」を押します。接続先はURLで指定した許可済みOriginです。ワーカートークンは管理PCの接続用で、ANAへ送信しません。
4. 管理PCのMileFinderとANAタブを開いたままにします。公開画面の接続状態と路線が反映されたら、スマートフォンや別のPCから条件を指定して会員照会します。

管理PC用画面は `member-worker=1` を指定したローカルURLで、接続先は公開サイトのOriginです。開発用に `api-origin` を明示することもできます。ANAタブと接続したウィンドウには `ana-member` と接続ごとの `ana-bridge` が追加されます。これらは管理用ブックマークレットが設定するため、手で接続IDを指定する必要はありません。通常の公開画面には管理者の接続操作を表示しません。トークンはローカルタブの `sessionStorage` で扱い、公開ページの保存領域へ置きません。URLのフラグメント経由で初期設定する場合は、読み取り直後にURLから除去します。ANAのタブにはトークンを渡しません。

終了するときは管理画面の「接続を停止」を押します。ANAとの接続を終了し、ローカルタブのトークンを削除します。再開時はANAのフライト一覧から接続し直してください。ブックマークレットによるANA接続の上限は30分で、管理画面側はその前の29分で公開接続を停止します。

ワーカートークンのローカル保管にはWindows DPAPIで暗号化した `.milefinder-worker-token.encrypted.local` を使います。`*.local` はGit管理対象外です。このファイルを配備アーカイブへ含めず、別の端末にコピーして認証情報を共有しません。

開発サーバーには管理PC専用の `POST /api/member-worker/local-config` があります。接続元のループバックIP、5173番ポートのHost、同一Origin・同一サイトのJSON要求を確認し、現在のWindowsユーザーで復号した管理用設定をローカル画面へ渡します。このAPIは `npm run dev` だけに組み込み、公開Workerや4173番ポートのプレビューには含めません。復号できない場合は手入力へ戻ります。トークン値をログへ出力しません。

## APIと保存データ

| API | 用途・認証 |
| --- | --- |
| `POST /api/ana/availability` | 公開画面と同じOriginから匿名チャットへ照会 |
| `GET /api/member/status` | 管理PCの接続状態・接続路線・処理中状態 |
| `POST /api/member/jobs` | 会員照会要求を登録。要求ごとの結果閲覧トークンを返す |
| `GET /api/member/jobs/:id` | 登録時のBearerトークンで結果と状態を取得 |
| `DELETE /api/member/jobs/:id` | 登録時のBearerトークンで自分の待機中要求の購読を取り消す。開始済みのANA通信は中断しない |
| `POST /api/worker/heartbeat` | 管理PCが接続状態を通知 |
| `POST /api/worker/claim` | 管理PCが次の照会を1件だけ受け取る |
| `POST /api/worker/jobs/:id/result` | 管理PCが検証済み結果または分類済みエラーを返す |

`/api/worker/*` は管理用Bearerトークンと、`http://127.0.0.1:5173` または `http://localhost:5173` のOriginを要求します。公開利用者へ管理用トークンを配る必要はありません。結果閲覧用トークンは要求ごとに発行し、D1には値そのものではなくハッシュを保存します。

D1には、管理PCの接続条件・最終通知時刻、会員照会条件・状態・検証済み結果・確認時刻、結果閲覧トークンのハッシュ、レート制限のカウンター、匿名照会の排他状態を保存します。レート制限では接続元IPのハッシュを利用し、IPの原文を保存しません。HTML全文・ANAの認証情報・会員番号・氏名は含めません。

会員要求の有効期間は10分、処理を割り当てる期間は90秒、待機を含む上限は8件です。同じ条件の取得済み結果は2分以内なら再利用し、元の確認時刻を維持します。照会要求・結果と結果閲覧トークンは作成後30分を過ぎると、後続のAPI受付時に削除します。定時の削除処理ではないため、無通信のまま30分ちょうどで物理削除される仕組みではありません。管理PCの接続条件は別の状態として保存します。

同じ条件を複数の利用者が照会した場合、1件の処理を共有します。待機中の照会を取り消しても他の利用者の購読が残っていれば処理は続き、最後の購読がなくなったときだけ待機中の処理を取り消します。処理開始後は、公開画面で結果の取得を止めてもANAへの通信を重ねて中断・再試行しません。

管理PCから15秒以上通知がないと未接続として扱います。処理期限切れやANA側エラーは未接続へ切り替え、管理者の再接続を待ちます。匿名チャットも公開サイト全体で同時1件へ制限しています。混雑時の待機・拒否は空席なしを意味しません。

## 取得範囲と残る制約

- 匿名チャットは会員の優先枠を反映せず、各方向最大7日単位で処理します。全便・全日・全クラスの網羅を保証しません。
- 会員照会は国際往復・エコノミー・成人1〜9名・各方向1区間のANA便が対象です。国内線、他クラス、乗り継ぎは未対応です。
- 会員単日の実取得と、表示済み結果の初回取り込みはローカルで成功しています。複数日連続取得では時間切れと空応答が発生しており、安定動作は未確認です。
- 初回の条件がANA画面と完全一致するときは、接続時の結果を1回だけ使います。取り込み時刻を保持し、新しくANAへ照会した結果とは表示しません。
- 公開画面で取得した結果は、その固定アカウントが照会できた範囲です。ほかの会員の優先枠や、実際に発券できることを保証しません。

## 公開構成の確認状況

2026年9月23日、`npm run build:cloud` の成功と、`npm run preview:cloud` によるローカルのworkerd・D1の起動を確認しました。`GET /api/member/status` は200で未接続状態を返し、管理PC向けCORSの事前確認は204を返しました。この確認はローカルのCloudflareランタイムでの動作であり、公開先からANAへ接続した実証とは区別します。
